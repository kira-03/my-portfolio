/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: { unoptimized: true },
    basePath: '/guruprasath-portfolio',
    assetPrefix: '/guruprasath-portfolio/',
};

export default nextConfig;

